[pytest]
# apps/voice is a separate Python environment from apps/api and needs its
# own config - without this file, running pytest from here would still walk
# up and find the root pytest.ini, whose pythonpath=apps/api makes
# `from app.main import app` resolve to the wrong app entirely.
testpaths = tests
pythonpath = .

asyncio_mode = auto
